server:
  port: 80

spring:
  application:
    name: consul-cloud-cosumer-order80
  cloud:
    consul:
      discovery:
        service-name: ${spring.application.name}
      port: 8500
      host: localhost
