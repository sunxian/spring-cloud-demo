server:
  port: 8006

spring:
  application:
    name: consul-cloud-provider-payment8006
  cloud:
    consul:
      discovery:
        service-name: ${spring.application.name}
      port: 8500
      host: localhost
